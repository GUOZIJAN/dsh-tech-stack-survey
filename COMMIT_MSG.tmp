feat: official dual-face package for permanent install

Package the plugin as a standards-conforming DSH dual-face npm package so it
can be installed permanently (README Option 2), alongside the dynamic
cordis_define path.

- package.json: dsh.client declaration (platform web + inject edges),
  exports "./client", peer deps (dsh-tools, dsh-user-questions, cordis,
  react), files list, npm scripts (test/check)
- lib/index.js: static Host half (named exports { name, inject, apply },
  ctx.tools.register(defineTool(...)) - the shipped dsh-tool-* idiom)
- lib/client.js: static Client half (lazy-CJS __ModuleLoader__ bundle
  exporting { inject, apply }, composer at priority -1 with dss_stack_ id
  selector)
- lib/types: .d.ts surfaces for both halves
- tests: static-host.test.js (real defineTool, execute flow, arg validation),
  client.test.js (bundle structure + composer select), host.test.cjs (renamed
  for type:module; custom-answer id aligned to dss_stack_)
- README.md / README.zh.md: bilingual docs incl. the verified permanent
  install + verification steps

Verified: node tests/static-host.test.js, tests/client.test.js, tests/host.test.cjs
all pass; live profile reports include:tech-stack-survey fiberPhase active.
